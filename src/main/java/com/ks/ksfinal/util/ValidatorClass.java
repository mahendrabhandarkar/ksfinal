package com.ks.ksfinal.util;
/*
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.stereotype.Component;
import org.springframework.validation.Errors;
import org.springframework.validation.Validator;

@Component
@Qualifier("validatorName")
public class validatorName implements Validator {

	public boolean supports(Class<?> clazz) {
		return viewModelClass.class.equals(clazz) || viewModelClass2.class.equals(clazz);
	}
	
	public void validate(Object target, Errors errors) {
		if(target instanceof ABCViewModel) {
			errors.rejectValue("parameter", "", "");
		}
		if(target instanceof XYZViewModel) {
			errors.rejectValue("parameter2", "", "parameter2 should not be null");
		}
	}
}

 */